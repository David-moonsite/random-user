import {Dimensions} from 'react-native';

export enum SCREEN {
  USERS = 'Users',
  USER_INFO = 'UserInfo',
}
export const MARGIN = 10;
export const NUMS_COLUMNS = 3;
export const {width} = Dimensions.get('window');
